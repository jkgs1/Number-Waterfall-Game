interface Player {
    id: string;
    name: string;
}
declare type Data = any;
interface Game<S extends any, P extends Player = Player, D extends Data = {}> {
    id: string;
    /** The name of this game */
    name: string;
    /** list of players */
    players: Array<P>;
    /** Is the game done */
    done: boolean;
    /** Has the game started */
    started: boolean;
    /** Winners, plural incase the game has a tie outcome */
    winners: Array<P>;
    /** The game's state, vary for different type of game */
    state: S;
    /** The generator used to advance to the next state */
    nextState(data: D): {
        error: string | null;
        valid: boolean;
    };
    /** Add the player to the game before starting, do nothing if already joined */
    join(p: P): void;
    /** Remove the player of the game, do nothing if player already left */
    leave(p: P): void;
    /** Start the game */
    start(): void;
    /** The game's history list, such that by calling `nextState(history)` will advance to the next state */
    history: Array<Data>;
}
export type { Game, Player };
